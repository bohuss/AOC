import kotlin.math.absoluteValue

class Day8 {
    val testAnswer1 = "1111111"
    fun part1(input: List<String>): String {
        val a = input[0].split(",").map{it.toInt()}.toIntArray()
        var ans = 0L
        for(s in input) {

        }

        return "$ans"
    }


    val testAnswer2 = "222222"
    fun part2(input: List<String>): String {
        val a = input[0].split(",").map{it.toInt()}.toIntArray()
        var ans = 0L
        for(s in input) {

        }

        return "$ans"
    }

}
